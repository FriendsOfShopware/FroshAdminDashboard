{
    "name": "frosh-admin-dashboard-tests",
    "private": true,
    "type": "module",
    "description": "End-to-end (Playwright) tests for the Frosh Admin Dashboard plugin.",
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "test:report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "1.60.0",
        "@shopware-ag/acceptance-test-suite": "^12.6.0",
        "@types/node": "^22.0.0",
        "dotenv": "^16.4.5"
    },
    "comment": "Pin a single playwright-core so the suite and @playwright/test share one type/runtime copy.",
    "overrides": {
        "playwright-core": "1.60.0"
    }
}
