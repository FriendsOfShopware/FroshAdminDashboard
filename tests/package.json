{
    "name": "frosh-admin-dashboard-tests",
    "private": true,
    "type": "module",
    "description": "End-to-end (Playwright) tests for the Frosh Admin Dashboard plugin.",
    "scripts": {
        "test": "playwright test",
        "test:ui": "playwright test --ui",
        "test:report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "1.60.0",
        "@shopware-ag/acceptance-test-suite": "^12.13.3",
        "@types/node": "^22.0.0",
        "dotenv": "^16.4.5"
    },
    "dependencies": {
        "kagami-playwright-reporter": "^0.0.2"
    }
}
